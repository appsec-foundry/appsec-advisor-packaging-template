build/
dist/
.reinit-backups/
upstream/
coverage.lcov

# Downloaded by the README quick-start curl command when tested locally.
/appsec-advisor-init.sh

.env
.venv/
__pycache__/
*.py[cod]

.DS_Store
.idea/
.vscode/

# Per-developer Claude Code permissions; the shared settings.json stays tracked.
.claude/settings.local.json

# Claude Code loads a root .mcp.json directly and it may contain tokens.
# Org-owned MCP servers belong in org-profile.yaml under mcp: instead.
.mcp.json
